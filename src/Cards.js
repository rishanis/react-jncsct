import React from 'react';

const Cards = (props) => {
  return (
    <div style={{backgroundColor: 'teal'}}>
      Home
      <img src={`${link}`} />
    </div>
  );
}

export default Cards;